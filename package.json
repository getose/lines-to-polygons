{
  "name": "lines-to-polygons",
  "version": "1.0.0",
  "description": "A JavaScript library to build polygons from an unordered list of independant lines.",
  "keywords": [
    "build",
    "polygons",
    "lines"
  ],
  "main": "lib/lines-to-polygons.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "test": "npm run build && mocha test --recursive"
  },
  "author": "Florent Muller",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.3.1",
    "chai": "^4.2.0",
    "mocha": "^5.2.0"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}
